---
operationId: get_shield_information_barrier_segment_members

summary: Get Shield Information Barrier Segment Members by Segment ID

tags:
  - shieldInformationBarrierSegmentMembers
  - get_shield_information_barrier_segment_members_by_segment_id

x-box-tag: shield_information_barrier_segment_members

x-box-scopes:
  - manage_managed_users

description: |-
  Get a list of Shield Information Barrier Segment Members by Segment ID.
parameters:
  - $ref: '../attributes/shield_information_barrier_segment_id.yml'
  - $ref: '../attributes/marker.yml'
  - $ref: '../attributes/limit.yml'

responses:
  200:
    description: |-
      Returns a paginated list of
      Shield Information Barrier Segment Member objects.
    content:
      application/vnd.box.internal+json:
        schema:
          type: object
          properties:
            pagination:
              type: object
              properties:
                prev_marker:
                  type: integer
                  description: |-
                    The marker for the start of the previous page of results.
                  example: 3000
                next_marker:
                  type: integer
                  description: |- 
                    The marker for the start of the next page of results.
                  example: 3000
            data:
              type: array
              items:
                $ref: |-
                 '#/components/schemas/ShieldInformationBarrierSegmentMember'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
