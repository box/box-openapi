---
tags:
  - ChunkedUpload
operationId: put_files_upload_sessions_id
summary: Upload Part
description: Upload a part of the file to this session.
parameters:
  - name: SESSION_ID
    in: path
    required: true
    schema:
      type: string
  - name: Digest
    description: |-
      The message digest of the partbody, formatted as specified by RFC 3230.
      The usage is sha=BASE64_ENCODED_DIGEST. Currently only SHA-1 is
      supported.As per RFC 3230, the output from SHA-1 algorithm must be Base64
      encoded.
    in: header
    required: true
    schema:
      type: string
  - name: Content-Range
    description: |-
      Byte range of part within overall file. E.g: bytes
      8388608-16777215/445856194. It must not overlap with the range of a part
      already uploaded to this session.
    in: header
    required: true
    schema:
      type: string
requestBody:
  content:
    application/octet-stream:
      schema:
        type: string
        format: byte
  description: Part bytes
  required: true
responses:
  '200':
    description: Part upload would be successful
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/UploadPartResponse'
  '400':
    description: Bad request. See response body for details.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
  '409':
    description: Part conflict. See response body for details.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
  '412':
    description: Preconditioned failed. See response body for details.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
  '416':
    description: Request range bit satisfiable. See response body for details.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
  default:
    description: Part upload error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
