---
title: Items

x-box-resource-id: items
x-box-reference-category: folders

description: |-
  A list of generic mini representations of a file, folder, user or enterprise

allOf:
  - $ref: '#/components/schemas/Pagination'
  - properties:
      entries:
        type: array
        items:
          $ref: '#/components/schemas/Item'
