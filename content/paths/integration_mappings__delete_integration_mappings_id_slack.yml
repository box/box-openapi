---
operationId: delete_integration_mappings_id

summary: Delete Slack integration mapping

tags:
  - integrationMappingsSlack
  - delete_integration_mappings_slack

x-box-tag: integration_mappings

x-box-scopes:
  - integration_mappings.readwrite

description: |-
  Deletes a Slack integration mapping
parameters:
  - $ref: '../attributes/integration_mapping_id.yml'

responses:
  204:
    description: |-
      Empty body in response
  404:
    description: |-
      Returns a `not_found` error if the integration mapping could not be found.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'
