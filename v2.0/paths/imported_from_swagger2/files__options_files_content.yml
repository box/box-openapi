---
tags:
  - Files
operationId: options_files_content
summary: File upload preflight check
description: |-
  The Pre-flight check API will verify that a file will be accepted by Box
  before you send all the bytes over the wire.
requestBody:
  $ref: '#/components/requestBodies/BoxFile'
responses:
  '200':
    description: The upload would be successful
  default:
    description: File upload preflight check error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
