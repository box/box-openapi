---
tags:
  - Legal Hold Policies
operationId: put_legal_hold_policies_id
summary: Update Existing Legal Hold Policy
description: |-
  Update existing Legal Hold Policy. Only name and
  description can be modified.
parameters:
  - name: ID
    in: path
    required: true
    schema:
      type: string
requestBody:
  $ref: '#/components/requestBodies/LegalHoldPolicy'
responses:
  '201':
    description: Returns information about the Legal Hold Policy updated.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/LegalHoldPolicy'
  default:
    description: Update Legal hold policy error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
