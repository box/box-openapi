---
operationId: delete_shield_information_barrier_segment_id

summary: Delete Shield Information Barrier Segment

tags:
  - Shield information barrier segments

x-box-tag: shield_information_barrier_segments

x-box-scopes:
  - manage_managed_users

description: |-
  Deletes the Shield Information Barrier Segment with specified ID.
parameters:
  - $ref: '../attributes/shield_information_barrier_segment_id--path.yml'

responses:
  204:
    description: |-
      Empty body in response

  404:
    description: |-
      Returns a `not_found` error if the Shield Information
      Barrier Segment with specified ID could not be found.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
