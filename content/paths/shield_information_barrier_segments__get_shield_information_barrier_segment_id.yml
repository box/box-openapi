---
operationId: get_shield_information_barrier_segment_id

summary: Get Shield Information Barrier Segment with specified ID

tags:
  - shieldInformationBarrierSegments
  - get_shield_information_barrier_segment_by_id

x-box-tag: shield_information_barrier_segments

x-box-scopes:
  - manage_managed_users

description: |-
  Get Shield Information Barrier Segment with the specified id.
parameters:
  - $ref: '../attributes/shield_information_barrier_segment_id--path.yml'

responses:
  200:
    description: |-
      Returns the Shield Information Barrier Segment object.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ShieldInformationBarrierSegment'

  404:
    description: |-
      Returns a `not_found` error if the
      Shield Information Barrier Segment could not be found.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
