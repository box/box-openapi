---
x-box-resource-id: task_assignment_collection

type: object

description: |-
  A list of task assignments

allOf:
  - $ref: '#/components/schemas/Pagination'
  - properties:
      entries:
        type: array
        items:
          $ref: '#/components/schemas/TaskAssignment'
