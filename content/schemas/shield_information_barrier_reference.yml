---
title: Shield information barrier reference

type: object

x-box-resource-id: shield_information_barrier_reference
x-box-tag: shield_information_barrier_reports

description: |-
  A Shield Information Barrier reference for requests and responses

properties:
  shield_information_barrier:
    type: object
    required:
      - id
      - type
    properties:
      id:
        type: string
        example: '1910967'
        description: |-
          The unique identifier for the shield information barrier
      type:
        type: string
        description: |-
          The type of the shield information barrier
        example: shield_information_barrier
        enum:
          - shield_information_barrier
    description: |-
      The `type` and `id` of the Shield Information Barrier.
