---
x-box-resource-id: metadata-patch

description: |-
  A [JSON-Patch](https://tools.ietf.org/html/rfc6902) specification for the
  changes to make to the metadata. The changes are represented as a
  JSON array of operation objects.

type: array

items:
  $ref: '#/components/schemas/MetadataPatchItem'
