---
title: Integration Mappings

type: object

x-box-resource-id: integration_mappings
x-box-tag: integration_mappings

description: |-
  Represents a list of integration mappings
allOf:
  - $ref: '../attributes/marker_pagination.yml'
  - properties:
      entries:
        type: array
        items:
          $ref: '#/components/schemas/IntegrationMapping'
        description: |-
          An integration mapping object
        example: "slack"
