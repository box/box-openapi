---
x-box-resource-display-name: Permissions (Writable)

x-box-resource-id: permissions--writable

description: |-
  An object defining if a file can be downloaded.

type: object

properties:
  can_download:
    description: |-
      Describes who is allowed to download this file
    type: string
    example: open
    enum:
      - open
      - company
