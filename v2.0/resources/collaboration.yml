---
allOf:
  - $ref: '#/components/schemas/Reference'
  - properties:
      type:
        type: string
        description: '`collaboration`'
        example: '`collaboration`'

      id:
        type: string
        description: The ID of the collaboration object
        example: "123456"

      item:
        allOf:
          - type: object
          - description: |-
              The file or folder that access is granted to
          - $ref: '#/components/schemas/ItemReference'

      accessible_by:
        allOf:
          - type: object
          - description: The user or group that is granted access
          - $ref: '#/components/schemas/User--Mini'

# invite_email:
#   type: string
#   description: |-
#     The email address used to invite an un-registered collaborator, if they
#     are not a registered user.

# role:
#   type: string
#   description: |-
#     The level of access granted. Can be `editor`, `viewer`, `previewer`,
#     `uploader`, `previewer uploader`, `viewer uploader`, `co-owner`, or
#     `owner`.

# expires_at:
#   type: string
#   format: date-time
#   description: When the collaboration will expire

# can_view_path:
#   type: boolean
#   description: |-
#     Whether the "view path collaboration" feature is enabled or not. View
#     path collaborations allow the invitee to see the entire parent path to
#     the item. View path collaboration does not grant privileges in any parent
#     folder (e.g. cannot see content the user is not collaborated on), other
#     than the permission to view the parent path.

# status:
#   type: string
#   description: |-
#     The status of the collaboration invitation. Can be `accepted`, `pending`,
#     or `rejected`.

# acknowledged_at:
#   type: string
#   format: date-time
#   description: |-
#     When the `status` of the collaboration object changed to `accepted` or
#     `rejected`
#
# created_by:
#   type: object
#   format: MiniUser
#   description: The user who created the collaboration object

# created_at:
#   type: string
#   format: date-time
#   description: When the collaboration object was created

# modified_at:
#   type: string
#   format: date-time
#   description: When the collaboration object was last modified

# created_by:
#   $ref: '#/components/schemas/UserReference'
# created_at:
#   type: string
#   format: date-time
#   description: The time this collaboration was created
# modified_at:
#   type: string
#   format: date-time
#   description: The time this collaboration was last modified
# expires_at:
#   type: string
#   format: date-time
#   description: The time this collaboration will expire
# status:
#   type: string
#   description: |-
#     The status of this collab. Can be accepted, pending, or rejected
# accessible_by:
#   $ref: '#/components/schemas/UserReference'
# role:
#   type: string
#   description: |-
#     The level of access this user or group has. Can be editor, viewer,
#     previewer, uploader, previewer uploader, viewer uploader, co-owner, or
#     owner
#   enum:
#     - editor
#     - viewer
#     - previewer
#     - uploader
#     - previewer uploader
#     - viewer uploader
#     - co-owner
#     - owner
# acknowledged_at:
#   type: string
#   format: date-time
#   description: When the status of this collab was changed
# item:
#   $ref: '#/components/schemas/ItemReference'
# can_view_path:
#   type: boolean
#   description: |-
#     Whether view path collaboration feature is enabled or not. View path
#     collaborations allow the invitee to see the entire ancestral path to
#     the associated folder. The user will not gain privileges in any
#     ancestral folder
