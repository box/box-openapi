---
tags:
  - Web Links
operationId: delete_web_links_id
summary: Delete Web Link
description: Deletes a web link and moves it to the trash
parameters:
  - name: WEB_LINK_ID
    in: path
    required: true
    schema:
      type: string
responses:
  '204':
    description: |-
      An empty 204 response if successfully deleted and moved to trash.
  default:
    description: Delete web link error
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
