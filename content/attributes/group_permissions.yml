---
type: object

description:
  The permissions that the authenticated user has for a group.

properties:
  can_invite_as_collaborator:
    type: boolean
    description:
      Specifies if the user can invite the group to collaborate on
      any items.
    example: true
