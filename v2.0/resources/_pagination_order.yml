---
description: |-
  The order in which a pagination is ordered

properties:
  by:
    example: 'type'
    type: string
  direction:
    type: string
    example: 'ASC'
    enum:
      - ASC
      - DESC
