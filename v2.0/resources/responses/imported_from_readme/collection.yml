---
properties:
  type:
    type: string
    description: '`collection`'
  id:
    type: string
    description: The ID of the collection
  name:
    type: string
    description: |-
      The name of the collection. The only collection available is named
      `Favorites`
  collection_type:
    type: string
    description: |-
      The type of the collection. This is used to determine the proper visual
      treatment for collections. The only collection type is `favorites`.
