{
  "operationId": "oauth2__get__authorize",
  "tags": ["Authorization"],
  "description": "Client-side OAuth 2.0 authorization",
  "parameters": [
    {
      "description": "The response type for this authorization flow",
      "name": "response_type",
      "in": "query",
      "required": true,
      "schema": {
        "type": "string",
        "enum": [
          "code"
        ]
      }
    },
    {
      "description": "The API client ID for your application",
      "name": "client_id",
      "in": "query",
      "required": true,
      "schema": {
        "type": "string"
      }
    },
    {
      "description": "The URL to redirect the user back to after they have completed their authorization",
      "name": "redirect_uri",
      "in": "query",
      "required": true,
      "schema": {
        "type": "string"
      }
    },
    {
      "description": "The URL to redirect the user back to after they have completed their authorization",
      "name": "state",
      "in": "query",
      "required": true,
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "scope",
      "in": "query",
      "required": false,
      "schema": {
        "type": "string"
      }
    }
  ],
  "responses": {
    "200": {
      "description": "Prompt the user to accept or reject this application access to their Box account.",
      "content": {
        "text/html": {

        }
      }
    }
  },
  "servers": [
    { 
      "url": "https://account.box.com/api/oauth2",
      "description": "Client-side OAuth 2.0 server"
    }
  ]     
}