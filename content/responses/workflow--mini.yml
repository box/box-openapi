---
title: Workflow (Mini)

type: object

x-box-resource-id: web_link--mini
x-box-tag: workflows
x-box-variants:
  - mini
  - standard
  - full
x-box-variant: mini

description: |-
  A Mini representation of a workflow response.

properties:
  id:
    type: string
    description: The unique identifier for the workflow
    example: "11446498"

  type:
    type: string
    description: "`workflow`"
    example: workflow
    enum:
      - workflow

  name:
    type: string
    example: "New Hire Workflow"
    description: |-
      The name of the workflow

  description:
    type: string
    description: |-
      The description for a workflow.
    maxLength: 256
    example: "This workflow sets off a new hire approval flow"

  is_enabled:
    type: boolean
    example: true
    description: |-
      Specifies if this workflow is enabled
