---
title: Shield information barrier segment member

type: object

x-box-resource-id: shield_information_barrier_segment_member
x-box-tag: shield_information_barrier_segment_members

description: |-
  A Shield Information Barrier Segment Member object

properties:
  id:
    type: string
    example: '11446498'
    description: |-
      The unique identifier for the
      Shield Information Barrier Segment Member

  type:
    type: string
    enum:
      - shield_information_barrier_segment_member

  shield_information_barrier:
    type: object
    properties:
      id:
        type: string
        example: '1910967'
        description: |-
          The ID of the Shield Information Barrier of this Member.

      type:
        type: string
        enum:
          - shield_information_barrier
    description: |-
        The `type` and `id` of the Shield Information Barrier of this Member.

  shield_information_barrier_segment:
    type: object
    properties:
      id:
        type: string
        example: '432554'
        description: |-
          The ID reference of the requesting Shield Information Barrier Segment.

      type:
        type: string
        enum:
          - shield_information_barrier_segment
    description: |-
      The `type` and `id` of the requested Shield Information Barrier Segment.

  user:
    allOf:
      - $ref: '#/components/schemas/User--Base'
      - description: The `type` and `id` of the
          requested Shield Information Barrier segment member.

  created_at:
    type: string
    format: date-time
    example: '2020-06-26T18:44:45.869Z'
    description: |-
      ISO date time string when this shield
      information barrier object was created.

  created_by:
    allOf:
      - $ref: '#/components/schemas/User--Base'
      - description: The user who created this
         Shield Information Barrier Segment Member.

  updated_at:
    type: string
    format: date-time
    example: '2020-07-26T18:44:45.869Z'
    description: |-
      ISO date time string when this
      Shield Information Barrier Segment Member was updated.

  updated_by:
    allOf:
      - $ref: '#/components/schemas/User--Base'
      - description: The user that updated
          this Shield Information Barrier Segment Member.
