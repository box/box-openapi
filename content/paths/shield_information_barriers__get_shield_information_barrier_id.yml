---
operationId: get_shield_information_barrier_id

summary: Get Shield Information Barrier with specified ID

tags:
  - shieldInformationBarriers
  - get_shield_information_barrier_by_id

x-box-tag: shield_information_barriers

x-box-scopes:
  - manage_managed_users

description: |-
  Get Shield Information Barrier with the specified id.
parameters:
  - $ref: '../attributes/shield_information_barrier_id.yml'

responses:
  200:
    description: |-
      Returns the Shield Information Barrier object.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ShieldInformationBarrier'

  404:
    description: |-
      Returns a `not_found` error if
      the Shield Information Barrier could not be found.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
