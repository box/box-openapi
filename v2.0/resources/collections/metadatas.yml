---
x-box-resource-id: metadata_collection

description: |-
  A list of metadata

x-box-reference-category: metadata

properties:
  entries:
    type: array
    items:
      $ref: '#/components/schemas/Metadata'
