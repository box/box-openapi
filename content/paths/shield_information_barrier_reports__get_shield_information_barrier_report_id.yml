---
operationId: get_shield_information_barrier_report_id

summary: Get Shield Information Barrier Report with specified ID

tags:
  - shield_information_barrier_reports
  - get_shield_information_barrier_report_by_id

x-box-tag: shield_information_barrier_reports

x-box-scopes:
  - manage_managed_users

description: |-
  Get Shield Information Barrier Report with the specified id.
parameters:
  - $ref: '../attributes/shield_information_barrier_report_id.yml'

responses:
  200:
    description: |-
      Returns the Shield Information Barrier Report object.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ShieldInformationBarrierReport'

  404:
    description: |-
      Returns a `not_found` error if the
      Shield Information Barrier Report could not be found.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
