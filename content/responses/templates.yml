---
title: Templates

type: object

x-box-resource-id: templates
x-box-tag: templates

description: |-
  A standard representation of a template, as returned from any Box Sign
  API endpoints by default.
allOf:
  - $ref: "../attributes/marker_pagination_with_prev_marker.yml"
  - properties:
      entries:
        type: array
        items:
          $ref: '#/components/schemas/Template'
