---
# yamllint disable rule:line-length

operationId: get_shield_information_barrier_segment_restriction_id

summary: Get Shield Information Barrier Segment Restriction by ID

tags:
  - shieldInformationBarrierSegmentRestrictions
  - get_shield_information_barrier_segment_restriction_by_id

x-box-tag: shield_information_barrier_segment_restrictions

x-box-scopes:
  - manage_managed_users

description: |-
  Get Shield Information Barrier Segment Restriction by ID.
parameters:
  - $ref: '../attributes/shield_information_barrier_segment_restriction_id.yml'

responses:
  200:
    description: |-
      Returns the Shield Information Barrier Segment Restriction object.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ShieldInformationBarrierSegmentRestriction'

  404:
    description: |-
      Returns a `not_found` error if the
      Shield Information Barrier Segment Restriction could not be found.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
