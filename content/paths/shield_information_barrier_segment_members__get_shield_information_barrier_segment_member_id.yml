---
operationId: get_shield_information_barrier_segment_member_id

summary: Get Shield Information Barrier Segment Member by ID

tags:
  - shieldInformationBarrierSegmentMembers
  - get_shield_information_barrier_segment_member_by_id

x-box-tag: shield_information_barrier_segment_members

x-box-scopes:
  - manage_managed_users

description: |-
  Get a Shield Information Barrier Segment Member by ID.
parameters:
  - $ref: '../attributes/shield_information_barrier_segment_member_id.yml'

responses:
  200:
    description: |-
      Returns the Shield Information Barrier Segment Member object.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ShieldInformationBarrierSegmentMember'

  404:
    description: |-
      Returns a `not_found` error if
      the Shield Information Barrier Segment Member could not be found.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'

  default:
    description: |-
      An unexpected client error.
    content:
      application/vnd.box.internal+json:
        schema:
          $ref: '#/components/schemas/ClientError'
